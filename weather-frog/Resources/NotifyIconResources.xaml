<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar">

    <ContextMenu x:Key="NotifyIconMenu">
        <MenuItem Header="Exit"
                  Icon="{DynamicResource ExitIconImage}"
                  Command="{Binding ExitAppCommand}" />
        <Separator />
        <MenuItem Header="Options"
                  Icon="{DynamicResource OptionsImage}"
                  Command="{Binding ShowOptionsCommand}" />
    </ContextMenu>

    <!--<tb:TaskbarIcon x:Key="NotifyIcon"
                    LeftClickCommand="{Binding ShowBalloonCommand}"
                    DoubleClickCommand="{Binding ShowBalloonCommand}"
                    DataContext="{x:Static Application.Current}"
                    ContextMenu="{StaticResource NotifyIconMenu}"
                    IconSource="{DynamicResource ExitDrawingImage}"
                    ToolTip="TMDb Db Cloner">
    </tb:TaskbarIcon>-->

</ResourceDictionary>
